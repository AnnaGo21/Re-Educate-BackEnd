-თეორია-
1)რამდენი ცვლადი გვაქვს ჯავასკრიპტში.(პასუხი თეორიულად გაეცი)
3 ცვლადი: let, const და var. 
const ცვლადის შეცვლა არ შეიძლება. ერთხელ განსაზღვრულს მუდმივად ის მნიშვნელობა აქვს.
let ცვლადის შეცვლა (გადააწერა) შეგვიძლია.
var js-ის ძველი ვერსიის ცვლადია და აღარ გამოიყენება, რადგან ბევრ პრობლემას იწვევდა იმის გამო, რომ
იყო function scoped, ანუ code block-ებს აიგნორებდა და code block-ის გარეთაც იყო ხელმისაწვდომი.


2)რამდენი ტიპი გვაქვს ჯავასკრიპტში.(ჩამოთვალე)(პასუხი თეორიულად გაეცი)
JavaScript-ში ტიპები იყოფა: პრიმიტიული ტიპი და რეფერენს ტიპი
პრიმიტიული ტიპები:
String
Number
Boolean
BigInt
Symbol
Undefined
Null

რეფერენს ტიპი:
Object

და object აერთიანებს: Array-ს, function-ს, Map-ს და ა.შ.


3) რომელი მეთოდი აქცევს სტრინგს მასივად.(პასუხი თეორიულად გაეცი)
split();
ეს არის ფუნქცია, რომელსაც გადაეცემა პარამეტრები თუ საიდან სადამდე მოჭრას და საბოლოოდ დააბრუნებს array-ის.
მაგალითად:
let text = "Hello World";

let arr = text.split(" "); ეს დაბეჭდავს ["Hello", "World"]


4) სტრინგი პრიმიტიული ტიპია თუ არა ? .(პასუხი თეორიულად გაეცი)
პრიმიტიული ტიპია ზოგადად, თუმცა მომენტებში როცა მასზე რამე მეთოდს ვიძახებთ, JS დროებით ქმნის String object wrapper-ს და
ამიტომ მუშაობს მასზე მეთოდები, როგორებიცაა toUpperCase(), trim(), slice(), ა.შ.

5)ჩამოთვალე რა მეთოდები ვისწავლეთ მაგ -> length(პასუხი თეორიულად გაეცი)
length
slice()
charAt()
toUpperCase()
toLowerCase()
concat()
split()
replace()
replaceAll()
trim()
includes()
indexOf()

loop-ები: for loop and while loop
ასევე ternary operator: 
isGoodStudent ? A : F

